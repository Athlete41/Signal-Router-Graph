@startuml
title [状态图 - B1→B2] STM32 LED BSP — 从业务到系统

' ══════════════════════════════════════════════════
' B1: 业务状态（外层）
' B2: 通过 !include 在状态内部展开子状态
' ══════════════════════════════════════════════════

state "关机" as Off

' ── B1:运行中 ── 展开到 B2 ──────────────
state "运行中" as Running {
  !include b2_running_states.puml
}

' ── B1:固件升级模式 ── 展开到 B2 ────────
state "固件升级模式" as DFU {
  !include b2_dfu_states.puml
}

' ── B1 层转换 ──
Off --> Running : 上电 / 硬件复位
Running --> DFU : 用户按下按键 (PA15)
DFU --> Running : 复位重启
Running --> Off : 断电

note left of Running
  【B1→B2 展开】
  主循环 + 中断处理
  （组件级子状态）
end note

note right of DFU
  【B1→B2 展开】
  USB断开 → LED快闪 → 等待DFU
end note
@enduml
