# 示波器节点 (OscilloscopeNode) 设计文档

## 一、概述

示波器节点是信号路由图编辑器中的可视化节点，接收二进制协议编码的波形数据（`QByteArray`），在工作线程中解析、缓存、处理，并实时绘制波形曲线。

**类似真实示波器**的功能：
- 显示实时波形曲线
- 时间窗口调节（水平缩放）
- 放大倍数 / 偏移调节（垂直缩放）
- 历史回看（滚动条）
- 缓冲区覆盖标记（红色垂直线）

---

## 二、架构

```
┌───────────────────────────────────────────────────┐
│                  主线程 (GUI)                      │
│  ┌─────────────────────────────────────────────┐  │
│  │ OscilloscopeContent                         │  │
│  │  ├── WaveformWidget  (QPainter 波形绘制)     │  │
│  │  ├── QScrollBar      (水平滚动)              │  │
│  │  ├── 采集系统 GroupBox  (FPS, Buffer)       │  │
│  │  ├── 水平系统 GroupBox  (时间窗口, 时间/格)  │  │
│  │  ├── 垂直系统 GroupBox  (放大倍数, 偏移)     │  │
│  │  └── 操作栏          (Start, 清空, 覆盖数)   │  │
│  └──────────────┬──────────────────────────────┘  │
│                 │ ▲ 信号/槽 (跨线程 AutoConnect)  │
│                 │ │                                │
│                 ▼ │                                │
│  ┌─────────────────────────────────────────────┐  │
│  │ _Worker (QObject, 在工作线程中)              │  │
│  │  └── OscilloscopeSampler                    │  │
│  │       ├── RingBuffer (环形缓冲区)            │  │
│  │       ├── QTimer (帧定时器)                  │  │
│  │       └── 帧计算逻辑                        │  │
│  └─────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────┘
```

### 2.1 线程模型

采用标准的 `_Worker + QThread` 模式：

1. `initUI()` 中创建 `_Worker` 对象和 `QThread`
2. 启动线程，用 `BlockingQueuedConnection` 调用 `initSampler()` 在工作线程创建 `OscilloscopeSampler`
3. 所有后续通信通过 PyQt5 信号-槽跨线程投递（`AutoConnection`，Qt 自动识别跨线程→异步投递）

### 2.2 渲染握手

```
工作线程                       主线程
   │                            │
   │  frameReady(data, ...) ──▶ │  _onFrameReady()
   │                            │    ├── WaveformWidget.setData()
   │                            │    ├── 更新滚动条
   │                            │    ├── 更新时间/格显示
   │                            │    ├── 更新覆盖计数
   │                            │    ├── repaint()
   │                            │    └── renderComplete.emit()
   │  ◀───────────────────────── │
   │  onRenderComplete()         │
   │     _awaiting_render = False│
   │     继续下一帧              │
```

核心目的：**防止工作线程的帧率超过主线程的渲染能力**。工作线程发射 `frameReady` 后设置 `_awaiting_render = True`，阻塞后续帧的发射，直到主线程完成渲染并回复 `renderComplete`。

---

## 三、环形缓冲区 (RingBuffer)

### 3.1 原理

定长数组 + 循环写入指针，当数据超出容量时自动覆盖最旧数据，并**记录覆盖位置**（`_overwrite_positions`）和**覆盖次数**（`_overwrite_count`）。

```
写入方向 →      write_idx
┌──┬──┬──┬──┬──┬──┬──┬──┐
│D5│D6│D7│D8│D1│D2│D3│D4│
└──┴──┴──┴──┴──┴──┴──┴──┘
              ↑           ↑
              最旧数据    最新数据
```

- `capacity`：固定容量（用户可调，默认 1000）
- `write_idx`：当前写入位置
- `count`：有效数据数（`< capacity` 时还在填充阶段）
- `_overwrite_positions`：自上次读取以来被覆盖的位置索引列表
- `_overwrite_count`：自启动以来总覆盖次数

### 3.2 可见窗口计算

```
时间窗口 (如1s)
┌────────────────────────────┐
│   可见数据 (visible_count)  │
├────────────────────────────┤
│◀─── scroll_offset ────────▶│
└────────────────────────────┘
←─── 全部数据 (total) ──────→
```

- `visible_count = time_window_s × 1_000_000 / sampling_interval_us`
- `scrollbar_max = total - visible_count`
- `scroll_offset = scrollbar_max - scrollbar_value`（scrollbar 最右=最新数据）

### 3.3 覆盖标记

当缓冲区被写满且新数据持续写入时，旧数据被覆盖。被覆盖的位置记录在 `_overwrite_positions` 中，每帧读取后清空（避免重复标记）。

**显示效果**：两条红色垂直线标记覆盖区域的最小和最大边界，提醒用户这个范围内的数据已被覆盖、不可信任。

---

## 四、协议格式 (waveform_protocol)

示波器和信号发生器之间通过 `connnodes/waveform_protocol.py` 定义的二进制协议通信：

```
| 帧头 2B | 采样间隔 4B | 数据数 2B | float32 数组 N×4B | 校验 1B |
| 0xAA55  | uint32 LE   | uint16 LE | float32 LE        | XOR     |
```

- **帧头**：`0xAA 0x55`，用于同步识别
- **采样间隔**：微秒（μs），每个数据点之间的时间间隔
- **数据数**：本次包携带的浮点数数量
- **数据**：float32 数组（小端序）
- **校验**：XOR，从帧头到最后一个数据字节

```python
# 编码示例（信号发生器侧）
from connnodes.waveform_protocol import encode_packet
qba = encode_packet(interval_us=1000, data=[1.0, 2.0, 3.0, ...])

# 解码示例（示波器侧）
from connnodes.waveform_protocol import decode_packet
result = decode_packet(qba)
# → {"sampling_interval_us": 1000, "data": [1.0, 2.0, 3.0, ...]}
```

---

## 五、UI 布局

示波器节点的 UI 分为五个区域：

### 5.1 波形画布 (WaveformWidget)

自定义 `QWidget`，用 `QPainter` 绘制：
- 黑色背景
- 暗色网格线（水平 × 垂直，5 格）
- 绿色波形曲线
- 底部时间轴标签（负值表示距当前的时间）
- 右上角显示每格时间（μs/div / ms/div / s/div）
- 红色垂直线标记覆盖区域

### 5.2 采集系统

| 参数 | 范围 | 默认 | 说明 |
|---|---|---|---|
| FPS | 1–120 | 30 | 帧定时器频率（每秒发射多少帧到主线程） |
| Buffer | 100–100000 | 1000 | 环形缓冲区容量 |

### 5.3 水平系统（时间轴）

| 参数 | 范围 | 默认 | 说明 |
|---|---|---|---|
| 时间窗口 | 0.01–60.0s | 1.0s | 画布上显示的数据对应的时间跨度 |
| 时间/格 | 只读 | — | 根据采样间隔和可见点数自动计算 |

### 5.4 垂直系统（幅值轴）

| 参数 | 范围 | 默认 | 说明 |
|---|---|---|---|
| 放大倍数 | 0.01–1000.0 | 1.0 | 数据幅值的乘法缩放 |
| 偏移 | -1000.0–1000.0 | 0.0 | 数据幅值的加法偏移 |

### 5.5 操作栏

| 控件 | 说明 |
|---|---|
| Start / Stop | 启动/停止帧定时器 |
| 清空 | 清空环形缓冲区和显示 |
| 覆盖: N | 显示自启动以来缓冲区被覆盖的总次数 |

### 5.6 视觉分组

三种 QGroupBox 用不同颜色的边框区分：

| 分组 | 边框色 | 用途 |
|---|---|---|
| 采集 | 蓝色 `#2a6a9a` | 采集参数 |
| 水平系统 | 青色 `#2a7a7a` | 时间轴参数 |
| 垂直系统 | 暖黄色 `#8a6a2a` | 幅值轴参数 |

---

## 六、关键参数详解

### 6.1 FPS（帧率）

控制工作线程每秒向主线程发送多少帧数据。**不是采样率**，采样率由数据源（信号发生器、串口等）决定。

- 高 FPS → 画面更新更流畅，但 CPU 消耗更高
- 低 FPS → CPU 节省，但画面更新有延迟感
- 推荐值：30（平衡流畅度和性能）

### 6.2 Buffer（缓冲区容量）

环形缓冲区的最大点数。

- 容量太小 → 数据快速被覆盖（红色标记频繁出现）
- 容量太大 → 内存占用高，滚动条范围大
- 选型参考：`buffer_size × 采样间隔 × 4bytes` ≈ 内存占用
- 例如：采样间隔 1ms，buffer=10000 → 显示 10 秒数据 → 内存 ~40KB

### 6.3 时间窗口

画布上同时显示的时间跨度。如果时间窗口内包含的数据点超过缓冲区容量，会自动限制为缓冲区大小。

- 短窗口 → 显示更多细节（放大）
- 长窗口 → 显示更长时间范围（缩小）

### 6.4 放大倍数 / 偏移

- **放大倍数**：对每个数据点 `value * amplification`，用于放大微小信号
- **偏移**：对每个数据点 `value + offset`，用于上下平移波形

公式：`display_value = original_value × amplification + offset`

---

## 七、数据流（完整路径）

```
信号发生器 / TCP / 串口等数据源
  │
  │ QByteArray (waveform_protocol 编码)
  ▼
OscilloscopeNode.inputs[0] (协议数据端口)
  │
  │ registerSlot → writeData (跨线程 AutoConnection)
  ▼
OscilloscopeSampler.writeData(data)
  │ decode_packet(data)  →  {sampling_interval_us, data}
  │ RingBuffer.write_batch(values, amplification, offset)
  ▼
QTimer 帧定时器 (每秒 FPS 次)
  │ _calc_visible_count()      ← 时间窗口 + 采样间隔
  │ _get_scroll_offset()       ← 滚动条位置
  │ RingBuffer.read_frame(vc, scroll_offset)
  ▼
frameReady.emit(data, overwrite_count, overwrite_positions, ms_per_div, scrollbar_max)
  │ (跨线程 AutoConnection)
  ▼
OscilloscopeContent._onFrameReady()
  │ WaveformWidget.setData()
  │ 更新 UI 控件
  │ repaint()
  │ renderComplete.emit()
  ▼
OscilloscopeSampler.onRenderComplete()
  │ _awaiting_render = False
  → 下一帧 tick 可继续发射
```

---

## 八、典型使用场景

### 8.1 信号发生器 → 示波器

```
[信号发生器] ──(QByteArray)──▶ [示波器]
```

- 信号发生器以固定频率生成波形数据（正弦波/方波/三角波/锯齿波）
- 编码为 waveform_protocol 后通过边发送到示波器
- 示波器解析、缓存、显示
- 最简单的测试方案

### 8.2 TCP 数据源 → 示波器

```
[TCP 服务端] ──(QByteArray)──▶ [示波器]
```

- 外部设备通过 TCP 发送符合 waveform_protocol 的数据
- TCP 服务端接收后通过边转发到示波器
- 适用于远程数据采集

### 8.3 串口数据源 → 示波器

```
[串口设备] ──(QByteArray)──▶ [示波器]
```

- 串口设备发送符合 waveform_protocol 的二进制数据
- 适用于嵌入式设备数据监控

---

## 九、序列化

示波器节点的状态会在保存/加载时序列化：

```python
# 序列化字段
{
    "fps": 30,              # 帧率
    "buffer_size": 1000,    # 缓冲区容量
    "time_window": 1.0,     # 时间窗口（秒）
    "amplification": 1.0,   # 放大倍数
    "offset": 0.0,          # 偏移
    "started": false,       # 是否已启动
}
```

反序列化时：
1. 恢复 UI 控件的值
2. 如果 `started == true`，自动启动帧定时器

---

## 十、性能考量

| 关注点 | 说明 |
|---|---|
| **帧率 vs 渲染** | 渲染握手确保主线程不被工作线程的帧率压垮 |
| **缓冲区大小** | 容量过大会增加 read_frame 和 scrollbar_max 计算开销 |
| **波形绘制** | QPainterPath 在大数据量（>10万点）时可能卡顿 |
| **跨线程通信** | 所有跨线程信号使用 AutoConnection，Qt 自动异步投递 |
| **覆盖标记** | 每帧读取后清空 `_overwrite_positions`，避免累积膨胀 |

---

## 十一、常见问题

### Q: 波形不动了？
A: 检查是否已点击 **Start**（按钮应显示 "Stop"），检查数据源节点是否正在发送数据。

### Q: 滚动条拖不动，总是自动弹回最右边？
A: 这是"自动跟踪最新数据"模式。如果滚动条在最右端（最大值），新数据到来时会自动跟进。要查看历史数据，向左拖动滚动条即可。

### Q: 红色垂直线是什么意思？
A: 红色垂直线标记了**缓冲区覆盖区域**的边界。当数据写入速度超过缓冲区容量时，旧数据被覆盖。两条红线之间的数据**已被覆盖一部分**，不能完整信任。增大 Buffer 参数可以减少覆盖。

### Q: 波形显示"断裂"或只显示部分？
A: 检查时间窗口设置是否合适。如果窗口太小，只能看到少量数据点；如果窗口太大，数据点过于密集。

### Q: 画面更新卡顿？
A: 降低 FPS（尝试 15 或更低），或减少缓冲区容量。如果数据点数很大（>100k），波形绘制本身也会变慢。
