# 示波器 V3

## 关于优化的大概准则
所有耗时的操作都应在其他线程进行，以避免阻塞 GUI 线程。

## 组件 + 线程 + 数据流

能大概体现性能优化方向, 大概符合优化准则。

```mermaid
flowchart TB
    subgraph GUI[主线程 GUI]
        subgraph C[OscilloscopeV3Content]
            UV[参数控件]
            subgraph WV[WaveformView]
                TM[刷新定时器]
            end
        end
    end

    subgraph DT[数据线程]
        DC[DataCore]
        A[(NPRingBuffer)]
        DC -->|写入 data| A
    end

    subgraph RT[渲染帧线程]
        RC[RenderCore]
    end

    DataNode ==o|数据: data: ndarray, interval_us: int | DC


    RC -.->|read_range| A
    WV -->|render_request: 相关参数| RC
    RC -->|数据准备完成信号 + 相关参数更新| WV
    UV <-->|参数交互例如fps等| WV
```

## 类图

大概指导如何编码与更细致的性能优化

```mermaid
classDiagram
    class ConnNode
    class ConnNodeContentWidget
    class QObject
    class QMutex
    class QPainterPath
    class object
    class QWidget

    QWidget <|-- WaveformView : 继承
    ConnNode <|-- OscilloscopeV3Node : 继承
    ConnNodeContentWidget <|-- OscilloscopeV3Content : 继承
    QObject <|-- DataCore : 继承
    QObject <|-- RenderCore : 继承
    object <|-- Ui_OscilloscopeV3 : 继承

    OscilloscopeV3Content *-- Ui_OscilloscopeV3 : 包含
    OscilloscopeV3Content *-- WaveformView : 包含
    WaveformView *-- QPainterPath : 包含
    OscilloscopeV3Content *-- _DataWorker : 包含
    OscilloscopeV3Content *-- _RenderWorker : 包含
    _DataWorker *-- DataCore : 包含
    _RenderWorker *-- RenderCore : 包含
    DataCore *-- NPRingBuffer : 包含
    RenderCore o-- NPRingBuffer : 包含
    RenderCore o-- WaveformView : 包含
    NPRingBuffer *-- QMutex : 包含
    OscilloscopeV3Node *--* OscilloscopeV3Content : 包含
    
```

| 类 | 基类 | 线程 | 职责 |
|---|---|---|---|
| `OscilloscopeV3Node` | `ConnNode` | GUI | 端口注册 |
| `OscilloscopeV3Content` | `ConnNodeContentWidget` | GUI | UI 控件、双线程管理、刷新定时器、渲染请求路由 - 请求处理渲染数据 |
| `DataCore` | `QObject` | 数据决策 | 写入存储器 |
| `RenderCore` | `QObject` | 渲染帧 | 渲染请求响应 - 准备渲染数据并通知 GUI |
| `NPRingBuffer` | 无（纯 numpy） | 跨线程 | 存储历史数据 |


## 实例连线图

更细致地指导如何编码与更细致的性能优化

> 一个 `OscilloscopeV3Node` 内部各运行时实例的引用关系。

```mermaid
flowchart LR
    N[OscilloscopeV3Node]
    C[OscilloscopeV3Content]
    UI[Ui_OscilloscopeV3]
    WV[WaveformView]
    DW[_DataWorker]
    RW[_RenderWorker]
    DC[DataCore]
    RC[RenderCore]
    R1[ring input_1]
    R2[ring input_2]

    PP1New[QPainterPath New input_1]
    PP2New[QPainterPath New input_2]
    PP1[QPainterPath input_1]
    PP2[QPainterPath input_2]


    N --> C
    C --> UI
    C --> WV
    C --> DW
    C --> RW
    DW --> DC
    RW --> RC
    DC -->|append| R1
    DC -->|append| R2
    RC -->|read_range| R1
    RC -->|read_range| R2

    RC -.->|build QPainterPath New Directly| WV
    WV -->|render path| PP1
    WV -->|render path| PP2
    WV -->|swap| PP1New
    WV -->|swap| PP2New
```



## 用户参数

| 项 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `x_window_ms` | float | 1000 | 时间窗口 |
| `y_window_mv_1` | float | 2000 | 输入 1 Y 轴窗口 |
| `y_offset_mv_1` | float | 0 | 输入 1 Y 轴偏移量 |
| `y_window_mv_2` | float | 2000 | 输入 2 Y 轴窗口 |
| `y_offset_mv_2` | float | 0 | 输入 2 Y 轴偏移量 |
| `x_div` | float | 1000us | X 轴格数 |
| `y_div` | float | 2000 | Y 轴格数 |
| `x_scroll` | int \| None | None | X 轴滚动位置（采样点索引）。None=自动滚动（窗口始终跟最新数据尾部），int=手动滚动到此索引位置。int 状态下拖到最右端自动回 None |
| `fps` | int | 30 | 渲染帧率上限 |
| `data_interval` | 只读 | 来自数据 | 由上游协议解析器提供 (`interval_us`)，非用户配置 |
| `memory_depth` | int | 10000 | 存储深度 |